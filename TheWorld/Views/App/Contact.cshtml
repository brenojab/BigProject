@model TheWorld.ViewModels.ContactViewModel
@{
  ViewBag.Title = "Contato";
}

@section scripts {
  <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
  <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
}
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h2>Contato</h2>
    <form method="post">
      @if (ViewBag.Message != null)
      {
        <div>@ViewBag.Message</div>
      }
      <div asp-validation-summary="ModelOnly"></div>
      <div class="form-group">
        <label asp-for="Nome">Name</label>
        <input asp-for="Nome" class="form-control" />
        <span asp-validation-for="Name" class="text-muted"></span>
      </div>
      <div class="form-group">
        <label asp-for="Email">E-mail</label>
        <input type="email" asp-for="Email" class="form-control" />
        <span asp-validation-for="Email" class="text-muted"></span>
      </div>
      <div class="form-group">
        <label asp-for="Mensagem">Mensagem</label>
        <textarea cols="40" rows="4" asp-for="Messagem" class="form-control"></textarea>
        <span asp-validation-for="Messagem" class="text-muted"></span>
      </div>
      <div class="form-group">
        <a asp-controller="App" asp-action="Index" class="btn btn-default">Cancelar</a>
        <input type="submit" value="Enviar mensagem" class="btn btn-success" />
      </div>
    </form>
  </div>
</div>
